# Ark · 方舟 —— 环境变量参考
#
# 注意：后端（apps/backend）不加载 dotenv，环境变量由运行环境注入
# （本机终端 export / PowerShell $env、systemd、Docker、Nginx 反向代理等）。
# 本文件仅作部署文档 + 配置样例；如需自动加载，可选用 Node 22 的 --env-file。
#
# 用法（本机，Git Bash）：
#   export PORT=4000
#   npm --prefix apps/backend run build
#   node apps/backend/dist/server.js

# ── 服务端口 ────────────────────────────────
# 后端监听端口（默认 4000，服务 host 固定 127.0.0.1）
PORT=4000

# ── 模型渠道（也支持通过 ARK_CHANNELS 多渠道）──
# 兼容 OpenAI 的 Base URL 与 API Key；未配置时走入「无模型」降级路径。
ARK_BASE_URL=https://api.openai.com/v1
ARK_API_KEY=sk-xxxx
ARK_MODEL=gpt-4o

# 多模型渠道（可选）：一行一个，逗号分隔的 JSON 数组
# 每个渠道：{ "name": 标识, "base": URL, "key": KEY, "model": 模型 }
# 例：ARK_CHANNELS='[{"name":"gpt","base":"...","key":"...","model":"..."}]'
# ARK_CHANNELS='[]'

# ── 认证 / 登录边界 ──────────────────────────
# 方舟的登录是「可选身份」而非硬门禁：
#   - 默认（不设任何变量）即「软门禁」：匿名可直接使用，数据落在全局(user_id=NULL)，
#     点左下角账号可选登录/注册；首注册用户自动成为管理员。
#   - 多人共用一个服务端（部署到服务器）时，让登录/账号体系发挥隔离作用。
#   - 若未来回到「硬门禁」（写请求必须登录），可引入 ARK_REQUIRE_AUTH=1 开关；
#     当前版本后端无硬门禁代码，此变量保留为未来扩展位，不影响现有行为。
# ARK_REQUIRE_AUTH=0

# ── 服务器部署提示 ───────────────────────────
# 网页轻端部署到服务器时：编译前端(NODE_ENV=production、VITE_API_BASE 指向后端域名)+后端，
# 用 systemd/Docker + Nginx 反向代理把 /api 与静态站点指向 127.0.0.1:4000。
# 桌面版在构建时由 apps/desktop/vite.config.ts 的 define 把 VITE_API_BASE 固定为
# http://127.0.0.1:4000（本地后端），无需在 .env 里再设。
