# 3 · 程序文件详解（Agent 架构详解）

> 逐个文件讲清楚：它做什么、关键函数/组件、和别的文件怎么协作。按层分层阅读。

---

## 一、后端 `apps/backend/src/`

### `server.ts` —— 入口
- 用 `buildApp()` 组装 Fastify 实例：注册 CORS、任务路由、成果下载、`/health` 健康检查。
- 底部直接 `listen({ port: 4000, host: "127.0.0.1" })` 启动（只监听本机，符合"数据不出本机"）。
- 成果下载 `GET /api/workspace/*` 用 `createReadStream` 流式返回，并做**目录穿越防护**（`normalize` + `startsWith` 校验）。
- **生产构建（M66）**：源码用 `moduleResolution:"bundler"` 可省 `.js` 扩展名，但 Node ESM 运行时必须带扩展名——`build` 脚本（`tsc -p tsconfig.json && node scripts/fix-esm.mjs`）先用 `tsc` 编译成 `dist/`，再用 `scripts/fix-esm.mjs` 把 dist 里 101 处 `from "../x"` 相对导入补上 `.js`，`node dist/server.js` 才真正能起。`dist` 保持分层目录，`store.ts` 用 `__dirname` 算出的 data 路径不受影响。

### `types.ts` —— 共享类型
- 定义 `Task` / `TaskStep` / `Artifact` / `SseEvent` 等，前后端以同一套字段约定对接（前端 `api.ts` 里的 `TaskDto` 与此对应）。

### `routes/tasks.ts` —— 任务 API
| 方法/路径 | 作用 |
|---|---|
| `POST /api/tasks` | 收 prompt → `randomUUID` 生成 id → **立即返回 `{taskId}`**，后台 `void runTask(id,prompt)` 异步编排 |
| `GET /api/tasks/:id` | 从 SQLite 读任务完整快照返回 |
| `GET /api/tasks/:id/events` | SSE：写 `text/event-stream` 头，订阅事件总线，回放历史 + 心跳防断连 |

> 关键设计：POST 不阻塞等待执行完，先返回 id，进度全靠 SSE —— 前端秒开任务页，步骤实时点亮。

### `agent/orchestrator.ts` —— 编排层（任务闭环大脑）
`runTask(id, prompt)` 流程：
1. 生成任务 → `insertTask` + 逐条 `insertStep`
2. `planTask(prompt)` 拆步骤（见 planner.ts）→ 广播 `plan` 事件 → `updateTaskStatus('running')`
3. 遍历每步：标 `running` → `sleep(900)` 模拟执行 → 最后一步**生成可编辑 Office 文件**（`tools/office.ts` 的 `genOffice`）到 `frontend/public/workspace/` → `insertArtifact`(deliverable) → 广播 `deliver`
4. 逐步置 checks ✓，标 `done`，广播 `check`/`done`
5. 把最终 checks/timeline 写回库 → `getTask` 返回最终快照
- **M31 断点续跑**：`runTask`（全新）与 `resumeTask`（重启恢复）共用内部 `runPlanSteps` 循环（startIdx + sections 注入）——每步置 done 时把产出整篇持久化进 `steps.note`；`resumeTask` 读已持久化步骤、把已完成步的产出接回 sections、从第一个未完成步续跑（不 reset），末步重新生成交付。**M64 起 `resumeTask` 从库重建 `refs` 参考资料传回 `runContentPipeline`**（`runTask` 已将 `opts.refs` 随 `insertTask` 落库），重启/续跑后「送给任务」的资料不再丢。

> 计划来源：`planner.ts`（LLM 优先 / 无 key 降级脚本计划器），`task.model` 标记「LLM/内置计划器」。

### `agent/planner.ts` —— 任务规划器
- `planTask(prompt)`：先尝试已配置 LLM（openai 兼容）拆 3~6 步 JSON；无渠道/失败降级**场景化脚本计划器**（PPT/Excel/Word/调研各自差异化步骤）。
- 返回 `{ steps, viaLLM }`，orchestrator 据此标记模型来源。

### `tools/office.ts` —— Office 文件生成
- `detectKind(prompt)`：按关键词选 PPT/Excel/Word。
- `genOffice(prompt, steps, outDir)`：用 pptxgenjs / exceljs / docx 生成**可编辑真实文件**并落盘，返回 `{name, kind, path}`。
- Excel 含原生公式（`COUNTA` 统计），PPT/Word 文字可编辑 —— 兑现"交付可编辑文件"卖点。

### `config/channels.ts` + `models/client.ts` —— 模型渠道
- `channels.ts`：**DB 持久化渠道**（SQLite `channels` 表），首启用内置 6 渠道/env 引导种子；`getChannels/getChannelById/getDefaultChannel` 读 DB；`createChannel/updateChannel/deleteChannel/setDefaultChannel` CRUD。
- `client.ts`：openai 兼容适配器，零依赖 fetch 调 `/chat/completions`（含 `chatStream` 流式），供 planner / chat 用。

### `models/router.ts` —— 模型路由
- `pickChannel()`：按各渠道**成功率**选最优（无记录按可用，有成功记录优先）。
- `recordSuccess/Failure(id)`：写 SQLite `channel_stats`，进程重启不丢。
- `getChannelStats()`：供 `GET /api/channels` 展示渠道 + 成功率。

### `agent/verifier.ts` —— 验收/重试
- `verifyDeliverable(path, kind)`：校验交付文件真实有效（存在 / 非空 / Office zip `PK` 头）。
- `verifyWithRetry(gen, maxTimes)`：生成后校验，不达标限次重试（最多 3 次），超限抛错。

### `tools/workspace.ts` —— 工作空间真扫描
- `scanWorkspace(dir)`：`readdirSync` + `statSync` 扫真实目录，返回文件列表（name/kind/size/sizeText/time，按大小倒序）。kind 由扩展名映射（pptx→ppt、xlsx→xls、docx→doc、md→md、png→png、html→html、pdf→pdf）。
- 供 `GET /api/workspace`（列目录）与下载联动；目录不存在时安全返回 `[]`。

### `tools/skills.ts` —— 技能管理（Markdown 即技能，落盘热加载）
- 每个技能一个 `<id>.md`，元信息（name/desc/enabled）写在开头 HTML 注释 front-matter；改文件即生效。
- `ensureSeed`（空目录写内置 5 技能）、`listSkills`（扫描解析）、`getSkill`、`writeSkillMeta`（整写回）、`toggleSkill`、`saveSkill`。
- 供 `GET/PUT /api/skills`、`/toggle`、C6 起 `DELETE`；**重启持久**（历史编辑保留）。C6 起 id 在白名单校验（字母/数字/下划线短横线），防 `${id}.md` 路径穿越。

### `routes/skills.ts` —— 技能 API
- `GET /api/skills` 列表；`GET /api/skills/:id` 含 code；`PUT /api/skills/:id` `{name,desc,enabled,code}` 保存（无 front-matter 时补默认，即**新建**）；`POST /api/skills/:id/toggle` `{enabled}`；C6 起 `DELETE /api/skills/:id` 删除 .md。

### `data/experts.ts` + `routes/experts.ts` —— 专家目录（C6 起内置 + 用户自建）
- `data/experts.ts`：**内置 5 位专家**（静态种子，只读，`builtin:true`）。
- `routes/experts.ts`：`GET /api/experts` 返回「内置 + 用户自建（experts 表，多用户隔离）」拼接；C6 起 `POST /` 创建、`PUT /:id` 编辑、`DELETE /:id` 删除自建专家（内置不可删）。

### `scheduler/jobs.ts` —— 自动化定时任务（本地 Cron）
- `jobs` / `job_logs` 两张 SQLite 表；CRUD（list/get/create/update/delete）+ `addLog`。
- `runJob(id)`：调 `runTask` 执行 action（复用编排闭环）产出真实文件并记成功/失败日志；M37 起 `resolveAction('template:<id>')` 解析模板，连同其 `AgentOptions`（expert/skills/modelHint）跑任务。
- `startScheduler()`：每 30s 轮询，`isDue` 匹配「每天 HH:MM 或 every:N 分钟」且启用 → 触发，同分钟防重。
- `describeSchedule`：把 schedule 转成可读「下次执行」文案。

### `routes/jobs.ts` —— 定时任务 API
- `GET /api/jobs` 列表；`POST` 新建；`PUT /:id`；`DELETE /:id`；`POST /:id/run` 手动执行；`GET /api/jobs/logs` 执行历史。

### `routes/connectors.ts` —— 连接器状态 API
- `GET /api/connectors`：真实状态。模型渠道（读 DB 报「N 个渠道·默认 X」）；Ollama 本地（`probeOllama` 真探 `GET localhost:11434/api/tags` 判在线）；联网搜索/内置浏览器/IM 桥（未接入，如实标未接入）。

### `routes/scenarios.ts` + `data/scenarios.ts` —— 场景库（WorkBuddy 式）
- `GET /api/scenarios`：返回 6 个场景 + 参考提示词。首页场景胶囊与提示词库页共用；前端 `listScenarios` 拉取（mock 仅离线降级）。

### `routes/channels.ts` —— 渠道 API
- `GET /api/channels`：渠道列表 + baseUrl + ok/fail/rate 成功率统计。
- `POST` 新增；`PUT /:id` 编辑；`DELETE /:id`；`POST /:id/default` 设默认；`POST /:id/test` 验活（GET `{baseUrl}/models` 判连通）。

### `routes/chat.ts` —— 对话 API
- `POST /api/chat`：SSE 逐 token 推送。有模型渠道 → 真实 LLM `chatStream` 流式；无 key/失败 → 内置回复逐字流式降级（`event: token`… 最后 `event: done`）。
- 前端 `sendChat()` 逐 token 渲染 + 流结束把完整回复沉淀进 history（多轮上下文）。
- **M40 对话式多轮任务执行**：命中工作意图（`runTask` 显式为真，或 `looksLikeTask` 检测「动作+产出物」）时，把该消息 `enqueueTask` 入队成**真实 Agent 任务**，SSE 先发 `event: task_created`（带 taskId），再 `done`（说明文本+taskId）。前端据此订阅该任务 SSE，气泡内渲染实时步骤进度 + 交付文件下载。休闲闲聊（只动作或只产出物）不会误触发。
- **M42 记忆注入对话 + `/记得` 命令**：`/记得 …`（可 `[偏好]/[事实]/[笔记]` 带标签）→ `createMemory` 落库，SSE 发 `memory_saved` 后 `done`，不进模型/任务；普通消息则用 `searchMemoriesFor`（抽 CJK 二元组 + 英文词 LIKE 高召回）把相关记忆抽进**系统提示**注入 LLM 上下文，SSE 发 `memory_ctx`（条数）供前端显示气泡。
- **M55 主动检索/召唤记忆**：新增 `/记忆 [关键词]` 命令（`parseMemRecall` 解析）——给关键词走 `searchMemories`（FTS 子串）、空结果再 `searchMemoriesFor`（高召回），留空列最近 8 条；SSE 回 `memory_search {items:[{id,kind,content}]}` 供前端渲染芯片。前端点芯片后消息夹带 `📌 引用记忆：<内容>`，`extractMemRefs` 抽出（去重）→ 作为**显式引用**注入模型上下文 + SSE 回 `mem_ref {count}`（此时不再发被动 `memory_ctx`）——用户点名要的记忆成为显式引用而非只靠自动猜。
- **M62 `/档案` 命令（对话内记忆全景）**：`parseProfile` 识别 `/档案` 前缀 → 聚合所有 preference/fact 记忆，`buildProfileItems`（标注过期 + 透出来源）后 SSE 回 `memory_profile {items:[{id,kind,content,source,expiredAt,expired}]}` + 文本「你的档案（N 条有效，M 条已过期）」摘要，`done` 后关闭。与 `/记得`（存）、`/记忆`（召）构成记忆三命令。

### `models/client.ts` —— openai 兼容 client
- `chat()`：非流式单轮补全。
- `chatStream()`：流式补全（`stream:true`），逐 `delta.content` 回调 `onDelta`，兼容 Ollama SSE 协议。供 chat 路由用。

### `agent/events.ts` —— 事件总线
- `subscribe(taskId, send)`：注册 SSE 发送器；返回取消函数。支持**回放**：新订阅者立即收到已发生事件。
- `publish(event)`：给该任务所有订阅者广播，并写入 `replay` 缓存。

### `db/store.ts` —— SQLite 存储层
- 用 Node 内置 `node:sqlite` 的 `DatabaseSync`；DB 路径可用 `ARK_DB_PATH` 覆盖（M25 起测试隔离用）。
- **表**：`tasks` / `steps` / `artifacts` / `spaces`（多工作空间 M11）/ `users`+`sessions`（登录 M12）/ `chat_sessions`+`chat_messages`（对话持久化 M19）/ `chat_messages_fts`（对话全文搜索 M23）/ `audit_log`（操作审计 M29）。
- **任务**：`insertTask`（含 `user_id`/`archived` 持久化；**M64 起可带 `refs` 参考资料落库**）/`getTask`/`insertStep`/`updateStepStatus`/`updateTaskStatus`/`insertArtifact`/`updateTaskChecksAndTimeline`/`resetTask`（幂等复位，供重试 M17）/`deleteTask`/`setTaskArchived`（归档 M22）/`listTasks`（多用户隔离 + 活跃/归档过滤）/`getUnfinishedTasks`（进程重启恢复 M24）/`countTasksByStatus`（统计 M18）/`recentTasks`/**`getTaskRefs`/`updateTaskRefs`（M64 读/写参考资料，resumeTask 续跑重建用）**。
- **对话**：`createChatSession`/`appendChatMessage`（同步入 FTS 索引）/`listChatSessions`/`getChatMessages`/`deleteChatSession`/`searchChatMessages`（FTS，按会话去重）/`reindexChats`（启动重建索引）。
- **记忆（M41 起）**：`createMemory`/`listMemories`/`getMemory`/`updateMemory`/`deleteMemory`/`searchMemories`（FTS5 trigram ≥3 字）/`findDuplicateMemories`（归一化归组，canonical 保留最早）/**`deleteMemories`（M57 批量删、多用户隔离）**/**`mergeMemories(keepId, removeIds, userId)`（M59：可见性校验 + 被删 tags 并进保留记忆 + 多用户隔离删除；**M63 补全——keep 无来源时继承被合并最早带来源的一条、`expires_at` 取最早过期（保守，并出来不超任一成员保鲜期）**，返回 `{kept, removed}`）**；`memories` 表 + `memories_fts` 索引同步更新。**M60 起表加 `source` 列（`PRAGMA table_info` 补列迁移）**：`createMemory` 收 `source`（manual / `chat:<sid>` / `chat-distill:<sid>`），get/list/searchMemories/searchMemoriesFor 全透出——记录每条记忆从哪来、可回链原会话。**M61 起表加 `expires_at`（epoch ms 可空）**：`createMemory` 收 `expiresAtMs`、`setMemoryExpiry(id, ms|null)` 设/清时效（多用户隔离）；自动注入 `searchMemoriesFor` 只取未过期（`expires_at IS NULL OR > now`），列表/召回仍见过期项——旧偏好不再被当恒真注入。
- **空间/用户/会话**：`listSpaces/createSpace/updateSpace/deleteSpace/getActiveSpace`；`createUser/findUserByUsername/verifyPassword/hashPassword`（scrypt）；`createSession/resolveSession/destroySession/pruneSessions`。
- **审计（M29）**：`appendAudit`（写一条，fire-and-forget）/`listAudit`（倒序可 limit）/`clearAudit`。
- **迁移逻辑**：`PRAGMA table_info` 检测缺列就 `ALTER TABLE` 补列（`steps.note`、`tasks.user_id`、`tasks.archived`、`spaces.user_id`、`channel_stats.latency_*`），幂等。

### `agent/events.ts` —— 事件总线（M21 起支持全局）
- `subscribe(taskId, send)`：注册 SSE 发送器，返回取消函数；支持**回放**（新订阅者立即收到已发生事件）。
- `subscribeAll(send)`（M21）：订阅**所有任务**的事件流，用于全局 SSE `/api/events`（任意页通知 + 侧栏刷新）。
- `publish(event)`：给该任务订阅者 + 全局订阅者广播，写 `replay` 缓存。

### `agent/runner.ts` —— 受控并发队列 + 失败兜底（M16）+ 重启恢复（M24）＋ 并发控制（M30）
- `enqueueTask(id, prompt, userId)`：**信号量槽位**受控并发（默认同跑 2，`ARK_MAX_CONCURRENCY` 覆盖），FIFO 排队；`getQueueStats()` 返回 `{active,queued,concurrency}`。
- `execute` 内 try/catch 包整个 `runTask`：任何异常 → `updateTaskStatus('failed')` + 广播 SSE `error`，不再卡 running。
- `resumeUnfinishedTasks()`（M24）：启动时把 `getUnfinishedTasks()` 命中的遗留 queue/running 任务重新入队；M31 起走 `enqueueResume` → `resumeTask`（断点续跑，不再从头重跑）。

### `agent/pipeline.ts` —— 多 Agent 内容管线（M10b）
- 分析师（提炼要点）→ 写手（展开 sections）→ 校验（收尾/补结论）三角色顺序执行，各 Agent 独立 LLM 优先 + 确定性脚本降级（20s 超时）。
- orchestrator 最后一步调 `runContentPipeline`，交付内容来自此管线；取代被删的 `llmContent.ts`。

### `tools/registry.ts` —— 工具注册表（M10）
- 统一接口 `{name, describe, run}`；`defaultTool` 内容整编工具。新工具=注册一个实现。
- orchestrator 每步经 Tool Registry 产出该步内容，注入最终 Office 交付。

### `tools/draft.ts` —— 无模型场景化草稿生成（M65）
- `draftSections(prompt, plan, kind)`：按 prompt 关键词判任务场景（报告/分析/调研、方案/计划、纪要/会议、培训/课件），生成**有开头总览、分节正文、结尾结论**的真内容骨架（含提示补充数据/指标的引导句），未命中回落通用骨架。写手无模型降级 `scripted` 走它（旧为 `defaultTool` 复读计划）——无 key 也能产出可直接编辑、读起来像真实初稿的交付文件。

### `tools/searchIndex.ts` —— 工作空间全文搜索（M20）
- `workspace_files`（文件名元数据）+ `workspace_files_fts`（FTS5 **trigram**）索引；`reindexWorkspace()` 启动时重建；`searchWorkspaceFiles(q)` 子串命中。
- 供 `GET /api/workspace/search`（必须在 `/api/workspace/*` 通配前注册）与 Sidebar Cmd+K。

### `util/log.ts` —— 结构化日志（M26）
- pino 配置 `base:{service:"ark-backend"}` + `enabled:!ARK_TEST`；导出 `loggerOptions`（给 Fastify 的配置对象）与 `taskLogger(taskId)`（任务执行用，taskId 作贯穿键）。
- orchestrator/runner 打点（task started/deliverable/finished/executionStarted/failed）。

### `models/router.ts` —— 模型路由（M14 起 failover + M15 加权 + M27 熔断）
- `pickChannel()` / `orderedChannels()`：按综合评分选优（可靠性50%+延迟20%+成本15%+优先级15%），**跳过被熔断渠道**。
- `recordSuccess/Failure/Latency(id)`：写 SQLite `channel_stats`；失败同时喂熔断计数。
- `isChannelOpen(id)`（M27）：连续失败 5 次 open + 10s cooldown + half-open 探测。
- `chatWithFailover(messages)`：按评分排序**逐个尝试所有渠道**直到成功，全败才抛错（上层降级）。
- `getChannelStats()`：供 `GET /api/channels` 展示渠道 + 成功率 + 延迟 + 评分。

### `routes/scenarios.ts` + `data/scenarios.ts` —— 场景库（WorkBuddy 式）
- `GET /api/scenarios`：返回 6 个场景 + 参考提示词。首页场景胶囊与提示词库页共用；前端 `listScenarios` 拉取（mock 仅离线降级）。

### `routes/auth.ts` —— 登录/注册 API（M12，软门禁）
- `POST /register` / `POST /login` / `POST /logout` / `GET /me` / `GET /status`（hasUsers+me）。密码 scrypt 哈希、session token 存 DB 7 天。
- `currentUser(req)`：读 Bearer token 解析当前用户，供任务/空间路由做资源隔离（传 user_id 过滤）。

### `routes/stats.ts` —— 统计仪表盘 API（M18）
- `GET /api/stats`：任务状态计数 + 最近任务 + 渠道健康（均值/最优评分），供前端 `/app/stats`。

### `routes/chat-sessions.ts` —— 对话会话 API（M19，M58 记忆沉淀）
- `GET /api/chat/sessions` / `GET .../:id/messages` / `DELETE .../:id`，供侧栏会话栏与 `?sess=` 恢复。
- **M58**：`POST .../:id/distill`（`tools/distill.ts` 提炼候选记忆，只返回不落库）+ `POST .../:id/distill/save {facts}`（确认后 `createMemory` 落库、多用户隔离、空 facts 400，注册在 `/:id` 之后不影响）。
- `tools/distill.ts`：`buildTranscript`（紧凑对话记录+截断）、`parseDistillCandidates`（解析「事实/偏好/笔记：内容」）、`heuristicCandidates`（无模型降级只抽带明确偏好信号的用户陈述）、`distillMemoryCandidates`（LLM 优先走 failover + 启发式兜底）。

### `routes/events.ts` —— 全局事件流 API（M21）
- `GET /api/events`：subscribeAll 订阅，把所有任务事件以 SSE 帧广播（15s 心跳），供任意页通知 + 侧栏刷新。

### `routes/audit.ts` —— 操作审计 API（M29）
- `GET /api/audit`：查看最近审计日志（倒序，可 `?limit=`，默认 200 上限 1000）。
- **根 onResponse 钩子**（导出 `installAuditHook`，在 server 根上下文按装）：统一审计所有 POST/PUT/DELETE 到 `/api/*` 的写请求——`currentUser` 归到当前用户（未登录记匿名），method+url → 可读中文动作 + 目标 id，写 `audit_log`。写请求无论成败都记（防抵赖）。**坑**：onResponse 是封装的，装在带 prefix 的插件里只审自己的路由，必须根上下文装（见 7-踩坑12）。

### `routes/queue.ts` —— 任务队列状态 API（M30）
- `GET /api/queue`：返回执行队列实时状态 `{active, queued, concurrency}`——正在执行 / 排队等待 / 配置并发上限（读 `getQueueStats()`）。供统计页/观测展示并发压测与排队情况。

### `maintenance/cleanup.ts` —— 自动清理（M32）
- `runCleanup()`：**按龄清理**终态(done/failed)非归档任务及其交付文件 + **审计裁剪**。取 `getCleanableTaskIds(olderThan)`（`tasks.created_ts < olderThan`，不碰 running/queue/归档）→ `listDeliverableFiles` 得交付文件名 → `safeUnlink`（只删工作目录内、防遍历穿越）→ 逐个 `deleteTask`；`audit_log` 超 `AUDIT_MAX` 则 `pruneAudit` 裁掉最旧。env 可配 `ARK_CLEANUP_ENABLED/TASK_DAYS/AUDIT_MAX`。返回汇总 `{enabled,tasksDeleted,filesDeleted,auditPruned,...}`。
- `routes/maintenance.ts`：`POST /api/maintenance/cleanup` 手动触发一次清理。

### `routes/templates.ts` —— 任务模板 API（M33）
- `GET /api/templates` 列表（多用户隔离：自己的+全局）、`GET /:id` 详情、`POST /` 创建、`PUT /:id` 更新、`DELETE /:id` 删除。
- `POST /:id/run`：**一键应用模板建任务**——取模板 prompt 经 `enqueueTask` 入队开始执行，并把模板的 `expert/skills/model` 作为 `AgentOptions` 传入（C2/M35：expert/skills 落进任务，model 作 modelHint 偏好渠道路由），返回 `{taskId}`。写请求自动落审计（应用任务模板/创建/编辑/删除任务模板）。
- 前端 `/app/templates` 页（`pages/Templates.tsx`）：列表 + 新建/编辑表单 + 删除 + 「应用并执行」按钮。

### `routes/guard.ts` —— 认证硬门禁（M36）
- `installAuthGate(app)`：**根上下文** preHandler——`/api/*` 的写请求（POST/PUT/PATCH/DELETE）要求有效会话，否则 401；读请求开放；`/api/auth/*` 放行自管。**默认开启**，`ARK_REQUIRE_AUTH=0` 退回软门禁。前端所有写调用已带 `authHeaders()`。

### `routes/users.ts` —— 管理端用户 API（M67，仅管理员）
- 首个注册用户自动成为管理员（`createUser` 里 `countUsers()===0`）；`users` 表加 `is_admin`/`disabled` 列（`PRAGMA table_info` 补列迁移）。`resolveSession` 对禁用用户返回空 → 禁用即踢下线（该用户所有走会话的请求即刻 401）。
- `GET /api/users`：每用户任务/记忆/会话计数 + 最近活跃（管理员专属，读也 gate 403）；`POST /:id/disable|enable`；`DELETE /:id`（`deleteUserWithData` 事务连带删该用户任务/步骤/产物/记忆/会话/会话消息/会话 token）。护栏：不能禁/删自己、不能禁/删最后一个管理员。
- store 新增：`listUsers`/`setUserDisabled`/`countAdmins`/`countRunningTasksByUser`/`deleteUserWithData`。

### `tools/fileVersions.ts` + `routes/versions.ts` —— 交付版本历史（M38）
- `snapshotDeliverable(workDir, name, taskId)`：orchestrator 每次产出交付后把当前文件快照进隐藏目录 `_ark_versions/<name>/<seq>-<name>`，并在 `file_versions` 表记一条（seq 同文件自增、倒序）。
- `listVersions(name)` / `rollbackVersion(workDir, name, seq)`：列出历史 / 把某历史存档复制回当前路径覆盖。`clearVersions` 配合清理。
- `GET /api/versions/:name` 列历史、`POST /:name/rollback {seq}` 回滚（写 → 走 M36 门禁）。`_ark_versions` 是目录，scanWorkspace 只列文件，天然不出现在资料库。
- 前端：Workspace 文件卡「版本」按钮 → 弹出历史面板 + 一键回滚。

### `routes/memories.ts` —— 记忆 API（M41，M57 批量整理）
- `GET /api/memories`（可 `?kind=` 过滤）、`GET /search?q=`（FTS5 trigram ≥3 字子串）、`GET /:id`、`POST /`（创建，content 必填 400）、`PUT /:id`、`DELETE /:id`；多用户隔离（自己的+全局）。注意 `/search`、`/duplicates` 注册在 `/:id` 之前，避免「search」被当 id 命中。
- **M57**：`GET /duplicates?kind=`（`findDuplicateMemories` 归一化归组，canonical 保留最早）+ `POST /batch-delete {ids}`（`deleteMemories` 批量删、多用户隔离、空 ids 400），都注册在 `/:id` 之前。
- **M59**：`POST /merge {keepId, removeIds}`——`store.mergeMemories`：先可见性校验（keepId 与全部 removeIds 均须对当前用户可见，任一不可见整体拒绝/404），再并把被删记忆 tags 并进保留记忆（去重、逗号拼接），最后 `deleteMemories` 删除重复；缺 keepId/removeIds → 400。注册在 `/:id` 之前。
- **M60**：`POST /` 接受可选 `source`（缺省默认 `manual`）——溯源字段，前端回链原会话用。
- **M61**：`POST /` 接受可选 `expiresAtMs`；`POST /:id/expiry {expiresAtMs}`（设/清时效，null=长期有效，不存在/不可见 404，须在 `/:id` 之前）。
- 存储：`memories` 表（id/kind/content/tags/user_id/created/**source**/**expires_at**）+ `memories_fts`（FTS5 trigram 索引，更新/删除同步）。前端 `/app/memories` 页（`pages/Memories.tsx`）：kind 筛选 pill + 防抖搜索 + 内联编辑器 + 卡片网格；**M57 起**卡片可勾选批量删除（选中顶部吸附「已选 N 条」批量条）+「查重」按钮展开查重面板（canonical 保留 + 重复打删除线可勾选 + 全选重复/全选整组），卡片升级暖卡；**M59 起**查重面板加「合并此组 / 合并全部」——一键把重复记忆并进 canonical（保标签并集），免逐条勾选；**M60 起**卡片加「来源」区——manual 显示「手动」标签，`chat:`/`chat-distill:` 渲染「来自对话/来自对话提炼」回链按钮，点击 `nav('/app/chat?sess=<sid>')` 打开来源会话；**M61 起**卡片加时效徽标（已过期 / N 天后过期 / 临近 7 天暖橙）+「延长 30 天 / 改为长期」按钮——过期记忆不再自动注入但仍可见可治理。

### `tools/web.ts` + `routes/tools.ts` —— 内置网页读取工具（M43）
- `tools/web.ts`：`webRead(url, {maxLength})`——仅 http/https，fetch + 去 script/style/tag + 实体解码抽正文可读文本，限长截断标注，失败返回 `error` 不抛。本地零依赖工具，供编排层引用资料。
- `routes/tools.ts`：`GET /api/tools` 工具清单（web.read 已就绪）、`POST /api/tools/web.read {url}` 真调用入口（缺 url 400，跨协议拦截，失败 502 返 error）。工具调用是写请求 → 走 M36 门禁 + M29 审计。

### `tools/knowledge.ts` —— 内置知识检索工具（M44）
- `searchKnowledge(query, userId, limit)`：零 embedding 的 RAG-lite——跨「记忆（`searchMemoriesFor` 高召回）+ 工作空间文件名（`searchWorkspaceFiles` FTS/LIKE）」检索相关片段，返回 `{source, kind, title, snippet, id?, space?}[]`；记忆多用户隔离、工作空间全局。
- `/api/tools/search.knowledge` 调用入口（缺 query 400）。工具调用是写请求 → 走 M36 门禁 + M29 审计（「知识检索工具调用」）。

### `routes/im.ts` + `db/store.ts`(settings) —— IM 消息桥（M45，默认关闭）
- `routes/im.ts`：`GET /api/im` 读配置、`PUT /api/im` 改配置（需登录，M36 门禁）、`POST /api/im` 入站 webhook——**默认关**（未启用 404）；启用后须带 `secret`（不匹配 403）；消息命中工作意图（`looksLikeTask`）→ `enqueueTask` 入队真实任务（handled:true+taskId），否则仅回执。入站 POST 在 `routes/guard.ts` 里豁免登录门禁（secret 即鉴权），配置 PUT 仍需登录。
- `db/store.ts`：通用 `settings` key-value 表 + `getImSettings/setImEnabled/setImName/rotateImSecret`（默认 enabled=0 + 随机 secret，数据不出机器）。
- 前端 `/app/im`（`pages/ImBridge.tsx`）：开关 pill / 接入方名称 / secret 复制+轮换 / 本地 webhook endpoint 展示。

### `tools/context.ts` + `agent/pipeline.ts` —— Web/检索工具接入编排管线（M46）
- `tools/context.ts`：`gatherContext(prompt, userId, refs=[])`——提示词内网址走 `web.read` 读正文（每源限长 2000）、本地知识走 `search.knowledge`（多用户隔离），两者缺省静默降级；`extractUrls` 提取 http/https（去重去尾标点最多 2 个）；`appendRefSection` 把采集结果追加为「参考资料」section（无引用不追加）。**M52：新增可选 `refs: AttachmentRef[]`（`{title?,url?,text}`）——「送给任务」时用户指定的参考资料，优先于自动抓取的 web/search 显示在 contextText 与参考资料 section；`AttachmentRef` 类型 + `PipelineContext.refs` 字段。**
- `agent/pipeline.ts`：`runContentPipeline` 先 `gatherContext`，把 contextText 注入 analyst/writer 的 LLM 提示，并在最终 sections 后追加参考资料；带 `userId` 与可选 `refs` 参数（多用户隔离）。
- `tools/office.ts` genDocx：渲染 `steps.length` 之外的多余 section（参考资料）——`...(sections ?? []).slice(steps.length).flatMap(...)`。
- `tools/searchIndex.ts`：`reindexWorkspace` 从「导入副作用」改为 server 启动显式调用（`!ARK_TEST` 时），避免测试并行对真实库重排导致 DB 锁。

### `tools/websearch.ts` —— 内置联网搜索工具（M47 search.web）
- `searchWeb(query)`：本地优先、默认免 key 走 DuckDuckGo HTML（`html.duckduckgo.com/html/?q=`），解析取回标题/链接/摘要（最多 8 条）；可选 `ARK_WEBSEARCH_ENDPOINT` + `ARK_WEBSEARCH_KEY` 接自配 provider（POST `{query}` → `{results:[{title,url,snippet}]}`，带 Bearer key）。出网限时 15s，任何失败返回 `{hits:[],error}` 不抛。
- `parseDuckDuckGo(html)`：`result__title` 拆块 + `result__a`/`result__snippet` 正则；DDG 链接是相对重定向 `//duckduckgo.com/l/?uddg=<真实URL>`，解 `uddg` 拿真实目标，`/^https?:\/\//` 守卫。数据经本机出网，符合「数据不出机器」原则下可选的联网能力。
- `routes/tools.ts`：`/api/tools` 清单新增 `web.search`（kind: search）+ `POST /api/tools/web.search {query}`（缺 query 400，异常 502 返 error）。写请求 → 走 M36 门禁 + M29 审计（「联网搜索工具调用」）。
- `tools/context.ts` `gatherContext`：未给网址且命中调研/搜索意图（调研/调查/竞品/市场/最新/趋势/搜索…）自动 `searchWeb(prompt)` 取回标题+摘要注入多 Agent 上下文，并收进「参考资料」section（失败静默降级）——与 web.read 组成「联网搜索 → 读正文」完整调研链路。

### `tools/browser.ts` —— 内置浏览器渲染工具（M49 browser.render）
- `renderPage(url, {maxLength, jsTimeout})`：playwright-core **wrap** 无头 Chromium 真渲染后再抽正文——优先 `main/article/[role=main]` 容器、fallback `body`（用 playwright 自带 `locator().innerText()`，避免在 Node 编译期引用 `document`/`HTMLElement`，后端 tsconfig 无 DOM lib），标题去 ` - ` 站点后缀。能拿到 web.read（只剥 HTML）拿不到的 **SPA / JS 客户端渲染**内容。
- `findChromeExecutable()`：本地复用 `ms-playwright` 缓存的 Chromium（探测 build 1234/1223/…，Windows/Linux 双路径），`ARK_CHROME_PATH` 可覆盖；找不到给安装提示不抛——**不联网自动下浏览器**，契合「本地优先」。
- `routes/tools.ts`：`/api/tools` 清单新增 `browser.render`（kind: read）+ `POST /api/tools/browser.render {url}`（缺 url 400，异常 502 返 error）。写请求 → 走 M36 门禁 + M29 审计（「浏览器渲染工具调用」）。
- `tools/context.ts` `gatherContext`：网址 `web.read` 报错或正文为空（疑似 JS 渲染）→ 自动 `renderPage` 兜底拿真实内容。
- 依赖：`playwright-core`（不带浏览器下载、体积小），浏览器复用本机缓存——需先装过 playwright（`npx playwright install chromium`）或已有缓存。

### `tools/save.ts` —— 保存到资料库（M50 tools.save）
- `saveToWorkspace({title,url,text,source})`：把正文以 Markdown 写进**当前活动工作空间**（`activeWorkspaceDir` 复用 `getActiveSpace`）；front-matter 含来源/保存时间，正文限长；写后 `snapshotDeliverable` 走版本快照（M38）。
- 写入带**路径穿越防护**：`safeJoin` 用 `resolve(join(base,rel)).startsWith(base+sep)` 校验，越界返回 error；文件名标题按非法字符清洗、加 `source-` 前缀与时间戳后缀。`ARK_WORKSPACE_ROOT` 可覆盖工作区根（测试隔离用）。
- 路由：`POST /api/tools/save {title,url,text,source?}`（tools.ts，写 → 走 M36 门禁 + M29 审计「保存到资料库」）。
- 前端：资料库搜索面板每条结果「存资料库」按钮 → `doSaveHit`（有正文用之，否则先 `webRead`）→ 存后刷新文件列表。

### `tools/searchSettings.ts` —— 联网搜索源设置（M50 副线 + M53 搜索源管理面板）
- `isWebSearchEnabled()`（settings 表 `websearch_enabled`，未设或 "1" → 开，默认开）、`setWebSearchEnabled(bool)`、`getSearchSourceStatus()`（{enabled, provider: duckduckgo|custom, endpointConfigured, ...}）。
- `searchWeb` 关停时直接返回 `{error:"联网搜索已关闭"}`；`web.search` 路由关停时 **403**；`gatherContext` 因 searchWeb 已短路故编排不再自动联网——符合「数据不出机器」可显式关停出网。
- **M53 provider 配置落库**：`getConfiguredSearch()`（库内 `websearch_provider/endpoint/key` 优先、`ARK_WEBSEARCH_ENDPOINT/KEY` env 兜底）、`updateSearchProvider(provider, endpoint?, key?)`（custom 必须给 endpoint，否则回落 duckduckgo）；`tools/websearch.ts` 改用 `getConfiguredSearch()` 选 provider（不再只认 env）。
- **M53 配额限流**：`getQuotaStatus()`/`tryConsumeQuota()`/`setQuota(rpm 1~6000)`——sliding window（settings `websearch_rpm/window_ts/window_count`，默认 30 次/分）；`searchWeb` 每次成功请求前 `tryConsumeQuota()`，达上限返回 `{error:"已达到本分钟搜索配额…"}` 且不发请求；`setQuota` 改配额即重置窗口。
- 路由：`GET /api/tools/search/source` + `POST /search/source/enabled` + `POST /search/source/provider {provider,endpoint?,key?}` + `POST /search/source/quota {rpm}` + `GET /search/source/quota`（写 → M36 门禁 + M29 审计「联网搜索源开关/配置联网搜索供应商/调整联网搜索配额」）。
- 前端：资料库搜索面板「联网·开/关」开关；**Settings 页「联网搜索」面板（M53）**——开关 + 供应商（DuckDuckGo 免 key / 自配 endpoint+key）+ 配额（次/分 + 实时剩余）+ 试搜（`webSearch` 验证配置）。

### `tools/refine.ts` —— 搜索结果后续加工（M54 总结 / 翻译）
- `refineText(kind, src, opts)` / `summarizeText` / `translateText`——把「已读到的正文」就地加工：总结（浓缩成 ≤maxLen 字要点）、翻译（转目标语言）。LLM 优先（内部 `llmOnce` 走 `chatWithFailover` 多渠道 failover，25s 超时），脚本降级保证开箱可跑：总结降级=截取正文开头，翻译降级=原样返回 + `error` 提示。
- 统一入口 `refineText(kind, src, {title?, lang?, to?, maxLen?})` 按 kind 分派；返回 `{kind, text, viaLLM, error?}`（viaLLM 标是否真模型）。
- 路由 `POST /api/tools/refine {kind,text,title?,lang?,to?,maxLen?}`（非法 kind / 缺 text → 400；M36 门禁 + M29 审计「资料加工（总结/翻译）」）。
- 前端：api.ts `refineText` 封装；`pages/Workspace.tsx` 搜索面板每条结果加「总结 / 翻译」按钮——`doRefine` 读（或现读，JS 页浏览器兜底）正文 → 加工，`wrefine` 内联展示产出（模型/脚本降级徽标）+「存入资料库」按钮（`doSaveRefined` 走 saveToWorkspace，标题加「（总结/翻译）」后缀）。

### `routes/tasks.ts` —— 任务 API（M17 起含生命周期）
| 方法/路径 | 作用 |
|---|---|
| `POST /api/tasks` | 收 prompt（M52 起可选 `refs:[{title?,url?,text}]`）→ id → **立即返回**，`enqueueTask` 入队后台串行执行；refs 过滤掉空正文后经 `AgentOptions.refs` 贯穿编排 |
| `GET /api/tasks` | 列表摘要（`?archived=1` 只列归档，多用户隔离） |
| `GET /api/tasks/:id` | 完整快照 |
| `GET /api/tasks/:id/events` | SSE 回放 + 心跳 |
| `POST /api/tasks/:id/retry` | 失败/完成任务用原 prompt 重新入队（幂等复位） |
| `DELETE /api/tasks/:id` | 删任务含步骤/产物 |
| `POST /api/tasks/:id/archive` | 归档/取消归档（运行中 409） |
| `POST /api/tasks/:id/template` | 另存为模板：done 任务一键固化为模板（继承 prompt/expert/skills/model，M34） |

> 关键设计：POST 不阻塞等待执行完，先返回 id，进度全靠 SSE —— 前端秒开任务页，步骤实时点亮。M16 起用 `enqueueTask` 受控并发队列（M30 起信号量，非直接 `runTask`）。

---

## 二、前端 `apps/frontend/src/`

### `api.ts` —— 前端 API 客户端
- 任务：`createTask(prompt)`→POST 返回 taskId；`getTask(id)` 快照；`listTasks(archivedOnly)` 摘要；`retryTask/deleteTask/setTaskArchived`（M17/M22）。
- 流式：`subscribeTask(id, handler)`（EventSource 按事件类型分发）；`subscribeGlobal(onEvent)`（M21 全局事件，任意页通知用）。
- 认证：`getToken/setToken`、`authHeaders()`（已登录则带 Bearer，资源按用户隔离）、`register/login/logout/getAuthStatus`（M12）。
- 空间/文件：`listSpaces/createSpace/deleteSpace/setActiveSpace/listSpaceFiles`；`searchWorkspace(q)`（M20 FTS）。
- 对话：`listChatSessions/getChatMessages/deleteChatSession`（M19）、`searchChatMessages(q)`（M23）。
- 统计：`getStats()`（M18）。
- 空 `BASE` 默认空串（走 Vite 代理 `/api`→4000），可用 `VITE_API_BASE` 覆盖。
- M48 联网：`webSearch(query)`、`webRead(url)` 封装 `POST /api/tools/web.search|web.read`（都走 `authFetch` 带登录态，失败返回 `{error}` 不抛，供资料库面板直用）。
- M51 浏览器兜底：`webRender(url)` 封装 `POST /api/tools/browser.render`（无头浏览器真渲染抽正文）——`webRead` 报错/正文为空（SPA/JS 渲染页）时的兜底。
- M52 送给任务：`createTask(prompt, refs?)` 新增可选 `refs` 参数（`[{title?,url?,text}]`），POST 建任务时携带参考资料。

### `pages/Workspace.tsx` —— 资料库（工作空间 + 联网搜索）
- 空间列表 + 文件树 + 成果网格 + M20 文件名全文搜索（`searchWorkspace`）。
- M48「联网搜索」面板（顶部卡片）：输入 query → `webSearch` 渲染**真实**标题/链接/摘要；每条「读正文」按钮 → `webRead` 内联展开正文（可收起、限高滚动），把 M47 后端能力在界面闭环。无蓝、暖米白卡片。
- M51 读正文浏览器兜底：`readOrRender(url)` 助手——先 `webRead`（快），报错/正文为空则自动 `webRender`（无头浏览器渲染，救 SPA/JS 页）；「读正文」与「存资料库」都用它，展开正文头部在用了渲染引擎时显示「浏览器渲染」徽标。
- M52 送给任务：每条搜索结果加「送给任务」按钮——`window.prompt` 输入任务指令，把正文（已有或 `readOrRender` 现读，JS 页也走浏览器兜底）作为 refs 调 `createTask(指令,[{title,url,text}])`，成功跳转 `/app/task?taskId=` 开始新任务。

### `layout/Sidebar.tsx` —— 侧栏（聚合导航 + 最近任务 + 全局搜索）
- 空间视图（M11 真实空间 CRUD）+ 最近任务（M9 接真，M21 起 done/error 自动刷新，M22 归档切换/右键菜单）+ 用户区（M12 登录弹窗）。
- **Cmd+K 全局搜索**：任务 + 工作空间文件（M20）+ 历史对话（M23） + 记忆（M56，`searchMemories` 搜 `mem_fts`、点击跳记忆页），弹板分四区。

### `pages/Home.tsx` —— 新会话首页（场景库入口）
- 顶部 6 个**场景胶囊** +「全部场景」；点开下拉列参考提示词，选中 `applyPrompt` 回填输入台（可移除标签）。
- 提交 `POST /api/tasks` → 成功 `nav("/app/task?taskId=...")`。

### `pages/TaskPage.tsx` —— 任务执行页
- `getTask` 打底快照 + `subscribeTask` SSE 实时点亮步骤/产物/清单；接住 M16 `error` 事件实时翻 failed。
- 终态任务：重试/删除按钮（M17）+ 归档按钮（M22）。

### `pages/Stats.tsx` —— 统计仪表盘（M18）
- 任务吞吐（各状态计数+占比条）、渠道健康（均值/最优评分）、最近任务动态。

### `pages/Chat.tsx` —— 对话（M19 起持久化）
- 左侧会话栏（切换/新建/删除，`?sess=` 恢复），发送续写当前会话。M40 起输入条加「执行」开关：开启后发送即把消息作为真实任务执行，assistant 气泡内渲染实时步骤进度 + 交付文件下载。
- **M55 记忆召唤芯片**：识别 `/记忆` 指令——收到 `memory_search` 事件后在输入框上方渲染记忆**芯片**（关键词命中/最近），点击把 `📌 引用记忆：<内容>` 夹带进输入框（可编辑后发送），实现「一键引用带入回复」；也设了「×」清空面板。
- **M62 档案面板**：`sendChat` 加 `onMemoryProfile` 分发；收到 `/档案` 的 `memory_profile` 事件后渲染档案面板——偏好/事实 pill（过期弱化）+「手动」标签 / 会话来源「回看来源」按钮（`nav('/app/chat?sess=<sid>')` 回原对话）+ 顶部计数 + 收起按钮——对话里即可看记忆全景。

### `layout/Layout.tsx` —— 应用壳
- 统一包 `/app/*`；M21 起 Chrome 浏览器通知（任务完成/失败，需授权）。

### 设计系统
- `index.css`：暖米白/茶褐设计令牌（`--bg` `--surface` `--brand` 等，禁蓝）。
- `shell.css` / `pages/pages.css`：壳与各页样式，含搜索弹板/统计/会话栏等。

---

## 三、文件协作图（一次任务的生命周期穿越的文件）

```
Home.tsx ──submit()──createTask──▶ api.ts ──POST──▶ routes/tasks.ts ──enqueueTask──▶ agent/runner.ts(受控并发队列)
                                                                    ▼
                                          agent/orchestrator.ts ──planTask(planner) + registry/office(真执行)──▶
                                              │ 落库/广播              │ events.ts (单任务 + 全局扇出)
                                              ▼                        ▼
                                          db/store.ts (SQLite)      SSE /api/events(M21)
                                                                      │
TaskPage.tsx ◀──subscribeTask── api.ts ◀──GET/events── routes/tasks.ts
     │ getTask(id) 快照 ◀──── db/store.ts        │ subscribeGlobal ◀── 任意页通知(M21)
```

---

## 三·五、交付与一键启动（M66）

真正"可交付落地"的出入口，收敛在仓库根。

### 根 `package.json` + `scripts/start.mjs` —— 一键本地启动器
- `npm run build`：先 `apps/backend` 再 `apps/frontend` 各 build 一遍（一并跑 fix-esm）。
- `npm start`（`node scripts/start.mjs`）：① 校验后端 `dist/server.js` 与前端 `dist/` 存在（缺则提示先 build，避免"白屏假可用"）；② `PORT=4000` 起后端；③ `node node_modules/vite/bin/vite.js preview --port 4173` 起前端预览（直调 Vite CLI JS 而非 spawn `npx`，规避 Windows `.cmd` shim 缺失）；④ 轮询后端 `/health` 通过后自动 `start` 打开浏览器到 `http://127.0.0.1:4173/app`；Ctrl+C 一并收尾。
- 说明：Windows 真实终端里 Node 收到 SIGINT 会正常 kill 子进程；Git Bash 后台化 `&` 会孤儿化孙进程，属测试方式差异非 bug。

### 前端 `vite.config.ts` —— 预览代理（M66）
抽出共享 `proxy`（`/api → http://127.0.0.1:4000`），同时给 `server.proxy` 与 `preview.proxy`——生产产物经 `vite preview` 也能把 `/api` 代理到本地后端，网页与桌面共用同一份后端地址。

### 桌面壳 `apps/desktop/vite.config.ts` —— 后端连通（M66）
桌面走 Tauri 自定义协议（无 Vite 服务器、无 `/api` 代理），相对 `/api` 接不到后端。用 `define` 在构建期把 `import.meta.env.VITE_API_BASE` 替换为 `http://127.0.0.1:4000`，让桌面包内前端显式指到本地后端（仍仅本机、数据不出机器）。原生 `tauri build` 需本机 Rust 工具链（cargo 尚未安装），属团队打包侧工作。

### 后端 `scripts/fix-esm.mjs` —— 生产 ESM 修复（M66）
`tsc` 编译后遍历 `dist/**/*.js`，把 `from "../x"` / `import("../x")` 的**相对路径**导入补上 `.js`（已带扩展名的不碰），幂等可重复跑。这是 `node dist/server.js` 能起的关键一环。

---

## 四、测试（M25 起，vitest）

- `apps/backend/src/**/*.test.ts`：store（任务/多用户/归档/未完成/FTS/空间/认证/**审计 appendAudit/listAudit/匿名**）、events（订阅/回放/全局）、runner（**并发控制/失败兜底/重启恢复**）、agent/orchestrator（**resumeTask 断点续跑**）、models/router（熔断）、test/http（Fastify inject：auth/任务/统计/对话搜索/**审计写请求落库**）、**test/acceptance（验收回归评测集 M28：PPT/Excel/Word 交付真实生成+合法+ExcelJS 读回+verifier 重试）**——**51 用例**。
- 隔离：`ARK_DB_PATH` 指向临时库、`ARK_TEST=1` 抑制 server 副作用，绝不碰真实 `data/ark.db`。
- 命令：`npm test`（vitest run）、`npm run typecheck`（`tsconfig.test.json` 含测试）、`npm run build`（dist 无测试）。
