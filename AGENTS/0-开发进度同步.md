# 0 · 开发进度同步（Agent 架构详解）

> 本项目的「最新进度」唯一权威入口。**每完成一步就回来更新本文件**。
> 回答三件事：现在做到哪、正在做什么、下一步做什么。配套各文详见文末索引。

---

## 一、当前进度总览（更新至 2026-09-11）

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M0 前端壳完整 | 官网 + 工作台 10 页，mock 数据可跑 | ✅ 既有 |
| M1 后端骨架 | Node+TS+Fastify+node:sqlite，健康检查 | ✅ |
| M2 任务真闭环 | POST /api/tasks → 拆步 → SSE → 交付下载 | ✅ 已接前端 |
| M3 真实 LLM planner | openai 兼容拆真步骤，无 key 降级脚本计划器 | ✅ |
| M4 真实 Office 交付 | PPT/Excel/Word 可编辑真实文件（pptxgenjs/exceljs/docx）| ✅ **E2E 三场景验证** |
| M4b 模型 router + verifier | 多渠道按成功率选优（router）；交付文件校验+限次重试（verifier）| ✅ |
| M5 对话流式 | POST /api/chat SSE 流式（真实 LLM / 内置降级）+ 前端 Chat 页接 | ✅ |
| M6 专家/技能/自动化 | 技能 Markdown 落盘热加载 CRUD + 专家 API + 自动化 Cron 定时任务 | ✅ |
| B1 模型渠道 CRUD | DB 持久化渠道（增删改/设默认/验活），与 router 打通 | ✅ |
| B3 连接器状态 | GET /api/connectors 真实状态（渠道计数 + Ollama 探测）| ✅ |
| M7 工作空间真扫描 | 真目录扫描（GET /api/workspace）+ 前端列表/下载/刷新 | ✅ |
| M8 桌面封装 | Tauri 2 复用前端 | 🔲 交给小伙伴封装 |
| M9 真实场景库/接真列表 | 场景库 GET /api/scenarios + 首页最近任务/侧栏接真实 backend | ✅ |
| M10 工具注册表 + 逐步真执行 | tools/registry.ts（{name,describe,run}）+ orchestrator 每步经工具产出内容、注入交付 | ✅ |
| M10a LLM 真内容 | tools/llmContent.ts 用已配模型生成交付正文，无门控降级脚本（后由 M10b 取代） | ✅ |
| M10b 多 Agent 管线 | agent/pipeline.ts 分析→写手→校验三角色，LLM 优先+降级；删除 llmContent.ts | ✅ |
| M11 多工作空间 | spaces 表 + CRUD + 活动空间 + 交付写活动空间目录；前端空间视图接真 | ✅ |
| M12 用户/登录 | users/sessions 表（scrypt）、auth 路由、前端登录/注册/当前用户 | ✅ |
| M13 多用户隔离 | tasks/spaces 加 user_id，路由按当前用户过滤/绑定，前端带 token | ✅ |
| M14 多渠道 failover | router.chatWithFailover 按成功率逐个试所有渠道，全败才降级；超时保护 | ✅ |
| M15 成本/延迟/优先级加权 | channels 加 priority/cost、channel_stats 加延迟；复合评分=可靠性50%+延迟20%+成本15%+优先级15% | ✅ |
| M16 任务串行队列 + 失败兜底 | runner.ts 模块级 Promise 链 queue（同时只跑一个）；异常→failed+SSE error，不再卡 running | ✅ |
| M17 任务生命周期 | POST /:id/retry（幂等复位重跑）+ DELETE /:id（含步骤产物）；TaskPage 加按钮 | ✅ |
| M18 统计仪表盘 | GET /api/stats + 前端 /app/stats（任务吞吐/渠道健康/最近动态） | ✅ |
| M19 对话持久化 | chat_sessions/chat_messages 两表 + ?sess= 恢复 + 侧栏会话栏 | ✅ |
| M20 工作空间全文搜索 | FTS5 trigram 文件名索引 + GET /api/workspace/search + Cmd+K 搜文件 | ✅ |
| M21 全局任务事件/通知 | GET /api/events 全局 SSE 扇出 + 浏览器通知 + 侧栏 done/error 自动刷新 | ✅ |
| M22 任务历史归档 | tasks.archived 列 + /:id/archive + 侧栏归档切换 + 右键/TaskPage 归档按钮 | ✅ |
| M23 对话深度搜索 | chat_messages FTS5 trigram 索引 + GET /api/chat/search + Cmd+K「历史对话」区 | ✅ |
| M24 进程重启任务恢复 | getUnfinishedTasks + resumeUnfinishedTasks 启动续跑（resetTask 幂等）| ✅ |
| M25 自动化测试 | vitest 套件（ARK_DB_PATH/ARK_TEST 隔离临时库）store/events/runner/HTTP 26→31 用例 | ✅ |
| M26 结构化日志 | util/log.ts pino（service 常驻 + taskId 贯穿）+ orchestrator/runner 打点 | ✅ |
| M27 多渠道熔断器 | router 每渠道熔断（5 次 open + 10s cooldown + half-open 探测）+ 单测 | ✅ |
| M28 验收回归评测集 | test/acceptance.test.ts：把「一句话→交付可编辑 Office」核心卖点固化为回归断言（PPT/Excel/Word 类型映射 + 真实生成/合法 zip/ExcelJS 读回 + verifier 重试 + defaultTool），12 用例 | ✅ |
| M29 操作审计日志 | audit_log 表 + 根 onResponse 钩子统一审计 POST/PUT/DELETE（登录/建任务/删渠道…，当前用户/匿名），GET /api/audit 可查 | ✅ 4 单测（store 3 + http 1），全量 47 用例 |
| M30 任务并发控制 | runner 从严格串行改信号量受控并发（默认 2，ARK_MAX_CONCURRENCY 覆盖，FIFO 排队）+ GET /api/queue 队列状态 + 统计页队列卡 | ✅ 2 单测（并发上限+并行、stats active/queued、env=1 严格串行），全量 49 用例 |
| M31 逐步断点续跑 | resumeTask 复用已持久化步骤（note 存产出），从第一个未完成步续跑、不 reset；runner.resumeUnfinishedTasks 改走 resumeTask | ✅ 2 单测（有断点续跑不重跑已完步、无断点退化为全新），全量 51 用例 |
| M32 自动清理策略 | tasks.created_ts（epoch ms）+ 按龄清理终态(done/failed)非归档任务及其交付文件 + 审计日志裁剪；`POST /api/maintenance/cleanup` 手动触发 + 调度器每 6h 自动（env 可配 ARK_CLEANUP_ENABLED/TASK_DAYS/AUDIT_MAX） | ✅ 5 store 单测 + 1 http 端点测试，全量 57 用例 |
| M33 任务模板 | 模板 CRUD + 一键应用模板建任务（预填提示词/专家/技能/模型） | ✅ 5 store 单测 + 2 http 端点测试（含应用模板建任务），全量 64 用例 |
| M34 任务另存为模板 | done 任务一键固化为模板（继承 prompt/expert/skills/model）；任务页「另存为模板」按钮 | ✅ 1 http 端点测试，全量 65 用例 |
| M35 模板字段真正生效 | 应用模板时 expert/skills 落进任务、model 作为 modelHint 偏好渠道路由（非排他） | ✅ 2 orchestrator 单测（带/不带 opts），全量 67 用例 |
| M36 认证硬门禁 | 写请求（POST/PUT/PATCH/DELETE→/api/*）需登录 401，读开放；`/api/auth/*` 自管；`ARK_REQUIRE_AUTH=0` 可回退软门禁 | ✅ 1 http 专用 gate 测试，全量 68 用例 |
| M37 定时任务可跑模板 | 自动化 job 的 action 支持 `template:<id>`（复用模板及其 expert/skills/model）；前端新建表单加「跑模板」下拉 | ✅ 3 jobs 单测（resolveAction），全量 71 用例 |
| M38 交付版本历史 | 每次产出交付快照进 `_ark_versions/<name>/` + `file_versions` 表；`/api/versions` 列表 + 回滚；工作空间页「版本」面板 | ✅ 3 store + 3 fileVersions 工具 + 2 http，全量 79 用例 |
| M39 Agent 角色可配置 | 专家由「静态内置」→「内置 + 用户自建落库 CRUD（experts 表，多用户隔离）」；技能页新增「新建 + 删除」（PUT 即可 upsert，DELETE 删 .md，加 id 防穿越）；专家页「创建专属专家」真正可用 | ✅ 3 store 单测 + 2 http 端点测试，全量 84 用例 |
| M40 对话式多轮任务执行 | 对话不再只是聊天——`POST /api/chat` 命中工作意图（`runTask` 或动作+产出物检测）时把该消息入队成真实 Agent 任务，SSE 回传 `task_created`；前端「执行」开关订阅任务 SSE，气泡内实时进度 + 交付文件下载 | ✅ 2 chat 单测（looksLikeTask），全量 86 用例 |
| M41 记忆系统 || M42 记忆注入对话+`/记得` || M43 内置网页读取工具 || M44 内置知识检索工具 || M45 IM 消息桥 || M46 Web/检索工具接入编排管线 | `tools/context.ts` gatherContext：任务提示词内网址→web.read 读正文、本地知识→search.knowledge 检索；注入 analyst/writer 的 LLM 提示 + 追加「参考资料」section 进交付文件（docx 渲染 overflow section）；`runContentPipeline` 带 userId 多用户隔离；`reindexWorkspace` 从导入副作用改为 server 启动显式调用 | ✅ 3 context 单测（extractUrls/appendRefSection/gatherContext 隔离）+ 管线单测验证产出参考资料 section + E2E（docx 含参考资料/本地资料/记忆内容），全量 110 用例 |
| M47 内置联网搜索工具 | `tools/websearch.ts` search.web——本地优先、默认免 key 走 DuckDuckGo HTML（`result__title` 拆块 + `result__a`/`result__snippet` 正则 + `uddg` 解码真实链接），可选 `ARK_WEBSEARCH_ENDPOINT/KEY` 接自配 provider；注册进 `/api/tools` + `POST /api/tools/web.search`（缺 query 400）；接入 `gatherContext`：未给网址且命中调研/搜索意图（调研/竞品/市场/最新/趋势…）自动联网取回标题+摘要，注入多 Agent 上下文并追加「参考资料」section（失败静默降级） | ✅ 4 websearch 单测（解析/默认免 key 成功/HTTP 500/空 query）+ 真实 HTML E2E（8 条命中、清华/央视/百度百科标题与真 URL 解码正确）+ tools 路由注入验证（成功 200/空 400/清单含 web.search/auth 401），全量 114 用例 |
| M48 资料库联网结果展示 | 前端闭环 M47：`api.ts` 新增 `webSearch()`/`webRead()` 客户端封装；资料库（`pages/Workspace.tsx`）顶部加「联网搜索」面板——输入 → `web.search` 渲染**真实**标题/链接/摘要，每条结果一个「读正文」按钮内联展开 `web.read` 正文（可收起），无蓝、暖白卡片 | ✅ 前端 build（tsc + vite）通过；后端注入 E2E 验证两条链路（web.search 命中 8 条真结果 + web.read 抽到标题/正文），全量 114 用例仍绿 |
| M49 内置浏览器渲染工具 | `tools/browser.ts` browser.render——playwright-core **wrap** 无头 Chromium 真渲染再抽正文（`main/article` 优先、fallback body），能拿到 web.read 拿不到的 **SPA / JS 客户端渲染**内容；本地复用 ms-playwright 缓存（`findChromeExecutable` 探测 1234/1223/…，`ARK_CHROME_PATH` 可覆盖，缺则给安装提示）；注册进 `/api/tools` + `POST /api/tools/browser.render`；接入 `gatherContext`：网址 web.read 报错或正文为空（疑似 JS 渲染）→ 自动 browser.render 兜底 | ✅ 3 browser 单测（探测/协议守卫/缺 URL）+ 真实无头渲染 E2E（本地 SPA 页 JS 填充内容被抽出、SPA 标题更新被读到）+ 路由注入验证（200/清单/400），全量 117 用例 |
| M50 搜到即存资料库 + 搜索源可控 | ① 存：`tools/save.ts` `POST /api/tools/save`——把上网读到的正文以 Markdown 写进**当前活动工作空间**（front-matter 来源/时间 + 版本快照 M38 + 写入路径穿越防 `resolve+startsWith(根+sep)`，`ARK_WORKSPACE_ROOT` 测试隔离）；前端搜索面板每条结果「存资料库」按钮（有正文用之，否则先 web.read）→ 存后刷新工作空间。② 搜索源可控：`tools/searchSettings.ts`（settings 表 `websearch_enabled` 默认开）+ `GET /api/tools/search/source` + `POST /api/tools/search/source/enabled`；关停后 `web.search` 路由 403 + `searchWeb`/`gatherContext` 不再自动联网（符合「数据不出机器」可显式关停出网）；前端面板「联网·开/关」开关 | ✅ 6 save/settings 单测（写 md/版本/空内容/标题清洗/默认开/开停）+ 路由注入 E2E（save 落盘+版本、source 状态、关停→web.search 403、重开），全量 123 用例 |
| M51 读正文浏览器兜底（前端闭环 M49） | 前端补上 M49 的唯一缺口：`api.ts` 新增 `webRender()`（POST /api/tools/browser.render 客户端封装）；资料库搜索面板「读正文」优先 web.read（快），**报错/正文为空（=SPA/JS 渲染页）→ 自动 `webRender` 无头浏览器渲染兜底**；「存资料库」同走该兜底（JS 页也能存到真正文）；展开正文头部加「浏览器渲染」小徽标标明用了渲染引擎 | ✅ 前端 build（tsc+vite）通过；后端真实 E2E：本地 SPA 页 `web.read` 只拿到加载壳（"数据加载中…"）而 `browser.render` 抽到 JS 渲染内容（"这是JS渲染的调研数据…"）——正是新兜底路径要救的场景；M50 起 123 用例仍绿（纯前端改动） |
| M52 送给任务（联网资料直达新任务） | M50「存资料库」之外的第二出口：把上到的真实资料**直接建一条新任务当参考资料**——搜索面板每条结果加「送给任务」按钮，弹窗输入任务指令，读（或已读）到的正文随 `POST /api/tasks` 的 `refs` 入参一路贯穿到编排：`gatherContext(..., refs)` 拼进 contextText + 追加「参考资料」section，多 Agent 看到用户指定的资料而非只靠自动抓取 | ✅ `tools/context` 2 个 refs 单测（注入 refs→上下文含正文+hasRef；appendRefSection 收进参考资料 section）+ HTTP 路由 E2E（POST /api/tasks 带 refs 201 返回 taskId、空正文 ref 过滤、缺 prompt 400）；前端 tsc+vite build 通过；全量 126 用例 |
| M53 联网搜索源管理面板 | 把 M50「搜索源可控」的 env 配置升级为**库内可编辑 + 节流配额**：`searchSettings.ts` provider 落库（`websearch_provider/endpoint/key`，库内优先、env 兜底；custom 无 endpoint 回落 duckduckgo）+ 配额滑动窗口（`websearch_rpm` 默认 30 次/分，`tryConsumeQuota` 超限不发请求返 error）；`tools/websearch.ts` 改用 `getConfiguredSearch()` 选 provider；路由 `GET /search/source` + `POST /:enabled` + `POST /:provider` + `POST&GET /:quota`；前端 Settings 页「联网搜索」面板（开关 + 供应商切换 endpoint/key + 配额次/分实时剩余 + 试搜） | ✅ 4 provider/配额 searchSettings 单测（默认 duckduckgo/custom 存读/custom 无 endpoint 回落/切回清理 hasKey）+ 3 配额限流单测（默认满额/消耗到限/钳制）+ 2 websearch 配额集成单测（超额返 error 不发请求/关闭返关闭）+ 3 HTTP 路由 E2E（source 状态/provider 切 custom 存读+非法 400/quota 钳制+缺 rpm 400）；前端 tsc+vite build 通过；全量 138 用例 |
| M54 搜索结果后续加工（总结 / 翻译） | 把「已读到的正文」就地二次加工成可复用资产：新增 `tools/refine.ts`——`refineText(kind, text, opts)` 经 `chatWithFailover` 走真实模型总结成要点 / 翻译（LLM 优先，脚本降级：总结=截取开头、翻译=原样返回+提示）；路由 `POST /api/tools/refine {kind,text,title?,lang?,to?,maxLen?}`；前端搜索面板每条结果加「总结 / 翻译」按钮——`refineText` 客户端封装 + `doRefine`（读（或现读）正文→加工）内联展示产出（模型/脚本降级徽标）+「存入资料库」按钮（`doSaveRefined` 复用 saveToWorkspace） | ✅ `tools/refine` 6 单测（总结走 LLM/空正文不调模型/LLM 失败脚本降级、翻译走 LLM/翻译失败原样返回、refineText 分派）+ HTTP 路由 E2E（非法 kind 400/缺 text 400/合法调用脚本降级返文本）；前端 tsc+vite build 通过；全量 145 用例 |
| M55 对话里检索/召唤记忆 | 把「记忆」从被动注入升级为**主动可召唤**：对话用 `/记忆 [关键词]` 命令显式检索记忆（给关键词走 FTS 子串、空结果再高召回 LIKE；留空列最近 8 条），SSE 回 `memory_search {items}`；命中记忆以**芯片**呈现在输入框上方，点击即在消息里夹带 `📌 引用记忆：<内容>`，后端 `extractMemRefs` 抽出来注入模型上下文并回 `mem_ref {count}`——用户点名要的记忆成为显式引用而非只靠自动猜 | ✅ chat 4 单测（parseMemRecall 两态 / extractMemRefs 去重+空）；live E2E 验证 `/记忆` 出 memory_search、引用标记出 mem_ref；前端 tsc+vite build 通过；全量 149 用例 |
| M56 全局搜索（Cmd+K）加记忆区 | 把「记忆」也纳入**全局搜索**：`Sidebar.tsx` 弹板跟随既有「工作空间文件 / 历史对话」模式，Cmd+K 输入即在 `mem_fts` 里 FTS5 trigram 子串搜记忆（拉 `searchMemories`），新增「记忆」分区渲染命中记忆（带 kind 标签），点击收起弹板并跳 `/app/memories`，无命中给空态文案——记忆与任务/文件/对话并列成为可全局检索的第五类资产 | ✅ 纯前端（复用 M41 已建 `/api/memories/search`，FTS 后端已验证）；前端 tsc+vite build 通过；全量 149 用例仍绿 |
| M58 对话记忆自动沉淀 | 在一段对话里提炼「值得长期记住的用户事实/偏好」，离开会话（新对话或切换）时**先预览、用户勾选确认后再落库**——新增 `tools/distill.ts`：`buildTranscript`（拼紧凑对话记录 + 截断）、`parseDistillCandidates`（解析「事实/偏好/笔记：内容」行、去重、过滤过短）、`heuristicCandidates`（无模型降级：只抽「用户消息 + 明确偏好/计划信号 + 非指令/提问」的陈述，宁缺毋滥）、`distillMemoryCandidates`（LLM 优先走 failover，失败启发式兜底，**只返回不落库**）；路由 `POST /api/chat/sessions/:id/distill`（返回候选）+ `POST /:id/distill/save {facts}`（确认后 `createMemory` 落库、多用户隔离、空 facts 400）；前端 Chat.tsx 在 `newChat`/`openSession` 离开会话时 `distillSession`，有候选则弹居中确认面板（默认全选可勾掉、跳过 / 记住所选），确认后 `saveDistilledFacts` 落库——**记忆沉淀由用户拍板，绝不未经确认写记忆** | ✅ distill 8 单测（buildTranscript 拼接/空、parseDistillCandidates 解析/过滤去重、heuristicCandidates 抽偏好跳过指令提问/上限去重空）+ HTTP E2E 2 条（distill/save 保存落库可检索+空 400、distill 返回候选+空会话 404）+ 前端 tsc+vite build 通过；全量 164 用例；live E2E：真会话 distill 抽出「暖色调偏好」→ save 落库为 preference 记忆并可查 |
| M59 记忆合并一键化 | 把「查重发现的重复记忆」从「手工勾选批量删」升级为**一键合并**：后端 `store.mergeMemories(keepId, removeIds, userId)`——先可见性校验（keepId 与全部 removeIds 都必须对当前用户可见，任一不可见整体拒绝），再把被删记忆的 tags 并进保留记忆（去重、逗号拼接），最后用 `deleteMemories`（多用户隔离）删除重复项，返回 `{kept, removed}`；路由 `POST /api/memories/merge {keepId,removeIds}`（缺 keepId/removeIds → 400、keepId 不可见/不存在 → 404）；前端记忆页查重面板加「合并此组 / 合并全部」按钮（组内一键并把重复合并进 canonical，或一次并完所有重复组；复用既有的 canonical 分组，免去逐条勾选的体力活） | ✅ store 2 单测（合并保 canonical + 标签去重拼接、keepId 不可见整体拒绝；FTS 检索 ≥3 字用「偏好暖色」）；HTTP E2E 1 条（merge 后保留记忆 tags 合并、重复删除；缺参数 400）；前端已并入 M58 build 一并通过；全量 167 用例；live E2E：两条同内容记忆 merge 后 tags 并成「偏好,设计,界面」、重复组归零 |
| M60 记忆来源溯源（回链） | 让每条记忆记住「从哪来」并可回链到原会话：`memories` 表加 `source` 列（`PRAGMA table_info` 迁移补列，向后兼容）——`manual`=手动画、`chat:<sessionId>`=/记得 沉淀于某会话、`chat-distill:<sessionId>`=M58 从某会话提炼；`createMemory` 收 source，`get/list/searchMemories/searchMemoriesFor` 四条读取路径全透出；各创建路径写入来源（memories.ts POST 缺省默认 manual、chat.ts `/记得` 记 chat:、chat-sessions.ts distill/save 记 chat-distill:）；前端记忆卡加「来源」区——manual 显示「手动」标签，会话来源渲染「来自对话 / 来自对话提炼」回链按钮，点击跳 `/app/chat?sess=<sid>` 打开原会话 | ✅ store 2 单测（source 四条读取路径透出 + 未传回读 null 向后兼容）；HTTP E2E 2 条（POST 显式 source 落库+缺省返 manual、distill/save 落库带 chat-distill 来源）；前端 tsc+vite build 通过；全量 171 用例；live E2E：POST 显式/缺省 source 均正确落库 |
| M61 记忆时效（过期） | 让「旧偏好不再被当恒真注入」：`memories` 表加 `expires_at`（epoch ms，可空，`PRAGMA table_info` 补列）；`createMemory` 收 `expiresAtMs`、`setMemoryExpiry(id, ms|null)` 设/清时效（多用户隔离，不存在/越权返 false）；**自动注入路径 `searchMemoriesFor` 只取未过期（`expires_at IS NULL OR > now`）**，`/记忆` 召回归类与 `list/get` 仍可见过期项供管理；路由 `POST /api/memories/:id/expiry {expiresAtMs}`（须在 /:id 之前）+ POST 支持 `expiresAtMs`；前端记忆卡加时效徽标（已过期 / N 天后过期 / 临近 7 天暖橙）+「延长 30 天 / 改为长期」按钮 | ✅ store 2 单测（expires_at 透出 + 注入过滤过期其余路径可见；setMemoryExpiry 设/清 + 不存在/越权 false）；HTTP E2E 1 条（POST 带 expiresAtMs、/:id/expiry 设/清、404）；前端 tsc+vite build 通过；全量 174 用例 |
| M62 对话内「档案」概览 | 让用户在对话里一句话看到「你记得我什么」：新增 `/档案` 命令（`parseProfile` 识别 /档案 及变体）——聚合该用户（软门禁下为全局）所有 **preference/fact** 记忆，`buildProfileItems` 标注是否过期并透出来源，SSE 回 `memory_profile {items}`（id/kind/content/source/expiredAt/expired）+ 文本「你的档案（N 条有效，M 条已过期）」摘要；前端 `sendChat` 加 `onMemoryProfile` 分发，Chat.tsx 渲染档案面板——偏好/事实 pill（过期弱化）+「手动」标签 + 会话来源「回看来源」按钮（`nav('/app/chat?sess=<sid>')`），收起可关——对话内即可管理记忆全景 | ✅ chat 单测 3 条（parseProfile 识别/排除普通消息；buildProfileItems 标过期+透出来源）；SSE 走注入不可用则 map 到单元测试；前端 tsc+vite build 通过；全量 176 用例；live E2E：/档案 返回 memory_profile 含 source/expired + done，连接正常关闭 |
| M63 记忆维护闭环（合并保来源/时效） | 收尾记忆维护的「合并丢失 provenance」缺口：`mergeMemories` 现在除并 tags 外，**一并继承来源溯源与时效**——若 keep 无来源（未知/手动），从被合并记忆中继承最早带来源的一条（避免合并丢失 provenance）；`expires_at` 取 keep 与被合并记忆里**最早过期**的一个（保守——并出来的记忆不会悄悄超出任一成员的保鲜期）；keep 已有来源/更早时效则不被覆盖 | ✅ store 单测 +2（无来源时继承来源+取最早过期；已有来源/更早时效不覆盖）；纯后端，前端 merge 交互不变；全量 178 用例 |
| M64 任务参考资料持久化（refs 闭环） | 修复「送给任务/联网资料的参考资料在进程重启或断点续跑后会丢」的真实缺口：`tasks` 表加 `refs` 列（`PRAGMA table_info` 补列迁移，存 AttachmentRef[] JSON）——`insertTask(task, userId, refs)` 落库、`getTaskRefs(id)`/`updateTaskRefs(id, refs)` 读/写；`orchestrator.runTask` 把 `opts.refs` 一并持久化（M52 的 POST /api/tasks 与 chat /执行 走同一入口，自动落库）；`resumeTask` 断点续跑时从库重建 refs 传回 `runPlanSteps`→`runContentPipeline`（含退化为全新 runTask 的兜底路径也带 refs）——多 Agent 看到重启前的参考资料，不再丢 | ✅ store 单测 +1（insertTask 落库 refs/getTaskRefs 读回/updateTaskRefs 覆写/不存在空数组）；orchestrator 单测 +1（resumeTask 从 getTaskRefs 重建 refs 传进 runContentPipeline，无 refs 传空数组）；全量 180 用例 |
| M65 无模型可交付（场景化草稿 + 开箱即用） | 修正「无 key/模型失败时交付文件只是复读需求+执行计划的空壳」的华而不实缺口：新增 `tools/draft.ts` `draftSections(prompt, plan, kind)`——按 prompt 关键词判场景（报告/分析/调研、方案/计划、纪要/会议、培训/课件），生成**有开头总览、分节正文、结尾结论**的真内容骨架（含提示补充数据/关键指标/责任人的引导句），未命中回落通用骨架；写手无模型降级 `scripted` 从旧 `defaultTool`（复读计划）改为走 `draftSections`——无模型也能产出**可直接编辑、读起来像一份真实初稿**的 PPT/Word/Excel | ✅ draft 单测 +4（报告场景含开头+结论、纪要命中待办、xls 按计划对齐行、未知场景回落）；前端 tsc 不受影响；全量 184 用例；live E2E：无模型运行「新能源汽车市场竞品分析报告」任务，生成的 .docx 含「背景与目标」开头节、逐节正文、「结论」收束——真实可编辑草稿而非复读计划清单 |
| M66 真正可交付落地（后端生产可启动 + 一键本地启动器） | 修掉「项目标榜可交付但 `node dist/server.js` 根本起不来」这个最大的华而不实缺口：**后端生产构建 ESM 扩展名修复**——源码用 bundler 解析（可省 `.js`），但 Node ESM 运行时必须带扩展名，`tsc` 编译产物里 101 处 `from "../x"` 无法解析导致 `npm start` 首次跑就崩（此前只用 tsx/vitest 验证过，从未真跑生产）。新增 `apps/backend/scripts/fix-esm.mjs` 在 `tsc` 后把 dist 相对导入补 `.js`；**前端预览代理**——`vite.config.ts` 抽出共享 `proxy` 并加 `preview.proxy`，生产构建产物经 `vite preview` 也能把 `/api` 代理到本地后端；**一键启动器**——根 `package.json` + `scripts/start.mjs`：一条命令先校验收产物，再起后端（`PORT=4000`）+ `vite preview`（`PORT=4173`，代理 /api）+ 健康探测通过后自动开浏览器到 `/app`，Ctrl+C 一并收尾。**桌面壳后端连通**——`apps/desktop/vite.config.ts` 加 `define` 把 `import.meta.env.VITE_API_BASE` 构建期钉死为 `http://127.0.0.1:4000`（桌面走自定义协议无 Vite 服务器，相对 `/api` 接不到后端） | ✅ 全量 184 用例仍绿；live E2E：`node dist/server.js` 起服健康 200；一遍 `npm run build` + `node scripts/start.mjs` 自动拉起后端+预览+代理，`/api/tasks`、`/api/spaces`、`/api/auth/status` 经 4173 代理全回 200，浏览器自动打开 `/app`；挂测一键 Ctrl+C 收尾 |
| M67 管理控制台（多人共用一个后端的运营看板） | 顺着「多人共用服务端」的定位把散落的管理能力收拢成一个 `/app/admin` 管理控制台（纯本地、数据不出机器）：**管理员机制**——`users` 表加 `is_admin`/`disabled` 列（`PRAGMA table_info` 补列迁移），**首个注册用户自动成为管理员**（自托管多人服务端合理默认），`resolveSession` 对禁用用户拒绝其会话（立即失效）；**用户管理 API `routes/users.ts`**（仅管理员可见，读也 gate）——`GET /api/users`（列表 + 每用户任务/记忆/会话计数 + 最近活跃）、`POST /:id/disable|enable`（不能禁自己/最后一个管理员）、`DELETE /:id`（连带删该用户全部数据 + 清会话；不能删自己/最后管理员）；**运维动作接入**——前端管理页可直接「手动清理」（复用 `POST /api/maintenance/cleanup`）+ 审计日志可视化（复用 `GET /api/audit`）；**audit 补管理动作标签**（禁用/启用/删除用户、手动清理）。前端**按用户设计图收敛到设置面板**（M68 修正，不设独立管理页/菜单入口）：**移除左侧菜单栏的「统计/记忆/管理」，底下账号区右侧加一个小齿轮直接进设置**；`Settings.tsx` 左侧导航新增「工具」组（**统计 / 记忆 / 用户管理**）内嵌渲染——统计内嵌 `Stats`、记忆内嵌 `Memories`、用户管理内嵌 `SettingsUsers`（账号/角色/用量 + 禁用/删除，复用 `routes/users.ts`）；删除独立 `Admin.tsx` 及其 `/app/admin(+audit/ops)` 子路由。暖色 UI、复用 `.settings-nav/.tcard/.job-table/.badge/.btn` 既有令牌，`.sec-embed` 中和子视图 `.page` 外边距。后端逻辑（管理员机制/用户 API/审计标签）不变 | ✅ store 单测 +4（首用户即管理员；listUsers 用量；禁用后会话失效；deleteUserWithData 连带清数据）、http 单测 +3（仅管理员可访问/普通 403、禁用后会话失效+不能禁自己/最后管理员、不能删最后管理员+可删普通用户）；全量 **191 用例/19 文件** 绿；live E2E（独立临时库真 HTTP）：首用户 `isAdmin:true` → 注册普通用户 `isAdmin:false` → admin 列用户清单带用量 → 普通用户访问 `/api/users` 403 → admin 禁用后其 `/me` 立即 401 → 匿名 `cleanup` 401；浏览器 DOM 验证：侧栏仅剩 新建任务/助理/专家·技能·连接器/自动化/任务模板/资料库/IM，设置导航含 统计/记忆/用户管理，底部小齿轮存在 |

 本地 webhook 网关 `/api/im`：**默认关闭**（settings 表 key-value，enabled/secret/name）；入站 POST 命中工作意图即入队真实任务，secret 充当鉴权（入站 POST 豁免 M36 登录门禁，配置 PUT 仍要登录）；前端 `/app/im` 配置页（开关/名称/secret 复制+轮换/本地 endpoint） | ✅ 3 store 单测（默认关+自动 secret/toggle+改名/轮换）+ E2E（默认关 404/门禁 401/开启/错 secret 403/建任务/非任务），全量 107 用例 |
 `tools/knowledge.ts` search.knowledge——零 embedding 的 RAG-lite：跨「记忆（searchMemoriesFor 高召回）+ 工作空间文件名（FTS/LIKE）」检索相关片段（source/kind/title/snippet），多用户隔离；注册进 `/api/tools` + `POST /api/tools/search.knowledge` | ✅ 3 knowledge 单测 + E2E（列表/检索/缺 query 400），全量 104 用例 |
 `tools/web.ts` 本地 fetch+去标签抽正文字本（http/https-only、限长截断、失败返回 error 不抛）；`/api/tools` 工具清单 + `POST /api/tools/web.read` 真调用入口 | ✅ 4 web 单测（协议/抽正文/title/截断/HTTP 错）+ E2E（列表/真实读取/协议拦截/缺 url），全量 101 用例 |
 `POST /api/chat` 命中 `/记得 …` 命令即存记忆并 SSE 回执 `memory_saved`；否则按相关度把用户记忆抽进上下文（`searchMemoriesFor` 抽 CJK 二元组+英文词 LIKE 高召回，优于 FTS 整句短语）；前端注入条数气泡 `memory_ctx` | ✅ 3 parseRemember 单测 + 3 store searchMemoriesFor 单测 + E2E（/记得 存 + 注入），全量 97 用例 |
 `memories` 表 + `memories_fts`（FTS5 trigram）CRUD/检索，多用户隔离；`/api/memories` 路由；前端 `/app/memories` 页（分类筛选/搜索/内联编辑/删除），无蓝、暖色偏好评级 pill | ✅ 4 store 单测 + E2E（建/列表/检索/改/删），全量 90 用例 |

## 二、正在开发什么

- **已刚完成（M24~M40 可靠性加固 + 治理 + 效率 + 门禁 + 版本化 + Agent 可配置 + 对话多轮）**：进程重启任务恢复（getUnfinishedTasks/resumeUnfinishedTasks）；vitest 自动化测试（M25 起，现 86 用例，揪出 insertTask 缺 archived 列、getUnfinishedTasks 忘 AS userId 两个真实 bug）；结构化日志（util/log.ts，taskId 贯穿）；多渠道熔断器（连续失败 5 次 open + 10s cooldown + half-open 探测，pickChannel/failover 跳过坏渠道）；验收回归评测集（M28）；操作审计日志（M29）；任务并发控制（M30）；逐步断点续跑（M31）；自动清理策略（M32）；任务模板（M33）；任务另存为模板（M34）；模板字段真正生效（M35）；认证硬门禁（M36）；定时任务可跑模板（M37）；**交付版本历史（M38：产物快照进 `_ark_versions/` + `file_versions` 表，/api/versions 列表与回滚）**；**Agent 角色可配置（M39：专家内置+用户自建落库 CRUD，技能页新建/删除，专家页「创建专属专家」可用）**；**对话式多轮任务执行（M40：对话里把消息跑成真实任务，SSE 回传 task_created，气泡内实时进度+交付下载）**；**记忆系统（M41：memories 表+memories_fts 全文检索，/api/memories 路由，前端记忆页分类/搜索/内联编辑，多用户隔离）**；**记忆注入对话+`/记得` 命令（M42：对话里 `/记得` 即存记忆回执；相关记忆自动抽进模型上下文，前端有注入气泡）**；**内置网页读取工具（M43：web.read 本地抽正文，/api/tools 工具清单+调用入口）**；**内置知识检索工具（M44：search.knowledge 跨记忆+工作空间 RAG-lite，多用户隔离）**；**IM 消息桥（M45：默认关闭的本地 webhook 网关，secret 鉴权，命中意图即入队任务，/app/im 配置页）**；**Web/检索工具接入编排管线（M46：gatherContext 采网址+本地知识，注入多 Agent LLM + 追加参考资料 section 进交付）**；**内置联网搜索工具（M47：search.web 本地优先免 key 走 DuckDuckGo HTML，可配 provider；调研/搜索类提示词自动联网注入标题摘要；/api/tools 清单+调用入口）**；**资料库联网结果展示（M48：api.ts 封装 webSearch/webRead；工作空间页「联网搜索」面板搜真实结果+点开读正文，前端闭环 M47）**；**内置浏览器渲染工具（M49：browser.render 无头 Chromium 真渲染抽正文，能拿 SPA/JS 客户端渲染内容；web.read 失败/空时自动兜底）**；**搜到即存资料库 + 搜索源可控（M50：把上网读到的正文一键存进当前工作空间（md+版本），前端「存资料库」按钮；联网搜索源默认开但可显式关停——websearch_enabled 开关 + web.search 403 + 编排不再自动联网）**；**读正文浏览器兜底（M51：前端 webRender 封装 + 读正文/存资料库遇 web.read 报错或空自动无头浏览器渲染兜底 + 「浏览器渲染」徽标，前端闭环 M49）**；**送给任务（M52：搜索面板「送给任务」一键建任务并把正文件为参考资料——POST /api/tasks 带 refs 贯穿 gatherContext 拼进上下文+追加「参考资料」section，用户指定资料直达多 Agent）**；**搜索源管理面板（M53：provider 配置从 env 升级为库内可编辑 + 滑动窗口配额限流，Settings 页可视化编辑；出租网能力默认开但可关停+限额）**；**搜索结果后续加工（M54：把已读到的正文就地总结/翻译——`tools/refine.ts` 经模型产出、脚本降级；搜索面板「总结/翻译」按钮内联展示并可直接存进资料库）**；**对话里检索/召唤记忆（M55：`/记忆 [关键词]` 主动检索记忆并以芯片呈现、点击夹带 `📌 引用记忆：` 让用户点名要的记忆成为显式引用注入上下文——从被动注入升级为可主动召唤）**；**全局搜索加记忆区（M56：Cmd+K 弹板新增记忆检索分区，输入即以 FTS5 trigram 搜记忆、点击跳记忆页——记忆与任务/文件/对话并列成为可全局检索的资产）**；**记忆暖卡+批量整理（M57：记忆页加「查重」面板自动归并重复记忆 + 卡片勾选批量删除；记忆卡升级为品牌暖卡并顺手修掉 `.pill.blue:hover` 的冷蓝——记忆从纯浏览进化为可整理）**；**对话记忆自动沉淀（M58：离开会话时把对话提炼成「值得记住的用户事实/偏好」候选，弹居中面板让用户勾选确认后落库——`tools/distill.ts` LLM 优先 + 启发式兜底，记忆沉淀由用户拍板、绝不未经确认写记忆）**；**记忆合并一键化（M59：查重面板「合并此组/合并全部」一键把重复记忆并进保留条目——`store.mergeMemories` 可见性校验 + 标签并集 + 多用户隔离删除，`POST /api/memories/merge`，免去逐条勾选批量删的体力活）**；**记忆来源溯源/回链（M60：`memories.source` 记录这条记忆从哪来——manual 手动 / `chat:<sid>` /记得 / `chat-distill:<sid>` M58 提炼；前端记忆卡显示「来自对话/来自对话提炼」回链按钮，点击打开来源会话——记忆不再是孤立条目，能追溯到产生它的上下文）**；**记忆时效/过期（M61：`memories.expires_at` 记录过期时间，自动注入 `searchMemoriesFor` 只取未过期的记忆（过期不再被当恒真注入），但列表/召回仍可见供管理；前端记忆卡标注「已过期/N 天后过期」+「延长 30 天/改为长期」——旧偏好可治理，不因堆积而失真）**；**对话内「档案」概览（M62：`/档案` 命令聚合所有偏好/事实记忆，SSE 回 `memory_profile` + 文本摘要，前端渲染档案面板——带偏好/事实 pill、过期弱化、「回看来源」回链按钮——对话里一句话就能看到「你记得我什么」）**；**记忆维护闭环（M63：`mergeMemories` 合并时一并继承来源溯源与时效——keep 无来源则从被合并记忆继承最早带来源的一条、过期取最早（保守），消除「合并后丢失 provenance」的缺口）**；**任务参考资料持久化（M64：`tasks.refs` 存「送给任务/联网资料」的参考资料 AttachmentRef[]，`runTask` 落库、`resumeTask` 断点续跑从库重建传回多 Agent——修复进程重启/续跑后参考资料丢失的真实缺口）**；**无模型可交付（M65：新增 `tools/draft.ts` 场景化草稿——按报告/方案/纪要/培训等场景生成有开头、分节、结论的真内容骨架，写手无模型降级从旧「复读计划清单」改走草稿生成——无 key 也能产出可直接编辑、读起来像真实初稿的交付文件）**；**真正可交付落地（M66：后端生产构建 ESM 扩展名修复——`tsc` 产物 101 处 `from "../x"` 是 bundler 解析遗留、Node ESM 起不来，新增 `scripts/fix-esm.mjs` 在 build 后补齐 `.js`，`node dist/server.js` 首次真跑通；前端 `vite.config.ts` 抽共享 proxy + 加 `preview.proxy`，生产产物经 `vite preview` 也能把 `/api` 代理到本地后端；根 `package.json` + `scripts/start.mjs` 一键启动器——校验构建产物→起后端+预览→健康探测后自动开浏览器到 `/app`，Ctrl+C 一并收尾；桌面壳 `vite.config.ts` 用 `define` 把 `VITE_API_BASE` 钉死为 `http://127.0.0.1:4000`——桌面走自定义协议无 Vite 服务器，相对 `/api` 接不到后端——打通壳到后端的连通）**；**管理控制台（M67：顺着「多人共用服务端」收拢散落的管理能力成 `/app/admin`——`users` 表加 `is_admin`/`disabled`，**首个注册用户自动成管理员**、禁用用户会话立即失效；`/api/users` 仅管理员可读可写——用户列表+用量、禁用/启用/删除（不能动自己/最后管理员，删除连带清数据）；管理能力随用户设计图收到设置面板——移除菜单栏「统计/记忆/管理」、底部账号区加小齿轮直进设置、设置导航加「工具」组内嵌 统计/记忆/用户管理、删独立 Admin 页与 `/app/admin*` 子路由；audit 补管理动作标签——自托管多人服务端的运营看板，唯一入口从「curl 才知道怎么管」变成「有个页面能点」）**。
- **正在做**：继续按路线图推进剩余进度（见 [5-企业级演进路线](5-企业级演进路线.md) 未勾选项）。

## 三、架构决策备忘（详见 8-变化原因）
- **后端**：Node 22 + TS + Fastify + 内置 `node:sqlite`（零编译，Windows 友好）。
- **编排**：先自写状态机，留 `plannerLLM` 接入点，按需演进（不提前上 LangGraph）。
- **进度推送**：SSE（单向持续推送 + replay 缓存）；M21 起有全局事件流（/api/events）供任意页通知。
- **LLM**：openai 兼容 fetch adapter，零依赖，DeepSeek/OpenAI/Ollama 通吃；M14 起 chatWithFailover 按成功率逐个试所有渠道，M15 加权（可靠性/延迟/成本/优先级），M27 加每个渠道熔断（5 次失败 open + 10s cooldown + half-open 探测）。
- **Office 交付**：pptxgenjs / exceljs / docx（全 JS，零原生编译），按需求关键词选类型，产出可编辑真实文件。
- **模型路由**：router 按渠道综合评分选优，统计写 SQLite；坏渠道自动降权 + 熔断短路。
- **验收/重试**：verifier 对交付文件做真实校验（存在/非空/zip），失败限次重试。
- **工作空间**：`GET /api/workspace` 真扫 `frontend/public/workspace/`，按类型/大小/时间列出；M20 起文件名 FTS5 trigram 全文搜索（Cmd+K 搜文件）。
- **对话流式**：`POST /api/chat` SSE 逐 token 推送；配 key 走真实模型 `chatStream`，无 key 走内置回复逐字流式降级；M19 起会话落库持久化、M23 起对话内容全文搜索。
- **自动化**：`scheduler/jobs.ts` 本地 Cron（jobs/job_logs 表），30s 轮询匹配「每天 HH:MM / every:N 分钟」到点调 runTask 并记日志；同分钟防重。M37 起 action 支持 `template:<id>` 定时跑模板（复用模板 config）。
- **模型渠道**：DB 持久化（channels 表，env 引导种子），CRUD + 验活（GET /models）+ 设默认；router/planner 直接消费 DB 渠道。
- **连接器**：`GET /api/connectors` 真实状态——渠道计数 + Ollama 探测（GET /api/tags），未接入的如实标未接入（不假在线）。
- **场景库**：`GET /api/scenarios`（`data/scenarios.ts` 6 场景），首页/提示词库共用；前端 `listScenarios` 拉取（mock 仅离线降级）。
- **任务执行可靠性（M16 起）**：M16 串行队列（runner.ts 模块级 Promise 链）→ M30 改信号量受控并发（默认同跑 2，ARK_MAX_CONCURRENCY 覆盖，FIFO 排队）+ 失败兜底（异常→failed+error，不卡 running）；M17 重试/删除；M24 进程重启恢复；M31 断点续跑（resumeTask）；M26 任务 ID 贯穿日志。
- **测试（M25 起）**：vitest（ARK_DB_PATH/ARK_TEST 隔离临时库，绝不碰真实 data/ark.db），store/events/runner/HTTP 10 文件 86 用例，npm test / typecheck(tsconfig.test.json) / build 三件套全绿。
- **认证（M36 起）**：写请求硬门禁（`routes/guard.ts` 根 preHandler：POST/PUT/PATCH/DELETE→`/api/*` 需登录 401，读开放；`/api/auth/*` 自管）；`ARK_REQUIRE_AUTH=0` 回退软门禁（本地单机/演示）。前端全部写调用已带 `authHeaders()`。
- **自动清理（M32 起）**：tasks.created_ts 存 epoch ms（created 是 zh-CN 本地化字符串不可排序）；`maintenance/cleanup.ts` 按龄删终态非归档任务的交付文件并从库清除，审计日志裁剪到 AUDIT_MAX；安全护栏 safeUnlink 只删工作目录内文件防路径穿越；`POST /api/maintenance/cleanup` 手动触发，调度器每 6h 自动，env 可开关/调留存。
- **任务模板（M33 起）**：`task_templates` 表（name/desc/prompt + 可选的 expert/skills/model）多用户隔离 CRUD + `POST /:id/run` 一键应用模板建任务（复用 enqueueTask 同管线）；前端 `/app/templates` 页（列表/新建/编辑/删除/应用并执行）。

## 四、文档索引（AGENTS/）
| 文档 | 内容 |
|---|---|
| [1-入门说明](1-入门说明-README.md) | 是什么、怎么跑、代码在哪 |
| [2-面试准备指南](2-面试准备指南.md) | 花式问答 + 硬技能清单 |
| [3-程序文件详解](3-程序文件详解.md) | 每个文件做什么、怎么协作 |
| [4-项目完整流程](4-项目完整流程.md) | 一句话 → 交付文件端到端 |
| [5-企业级演进路线](5-企业级演进路线.md) | 单机 → 企业级分阶段路线 |
| [6-架构技术栈图](6-架构技术栈图.md) | 分层架构图 + 选型原因 |
| [7-踩坑日志](7-踩坑日志.md) | 踩过的坑：现象→原因→解决 |
| [8-变化原因](8-变化原因.md) | 为什么从 A 变 B |
| [9-排查日志](9-排查日志.md) | 排查路径 / 方法 |

> 说明：`AGENTS/`（及本目录）是**跟踪入库**的权威版本；`docs/agent-architecture/` 下的同名文件是开发过程中的**工作副本**（gitignored、内容近似但未入库）。**以 `AGENTS/` 为准，且每完成一个里程碑都须同步 `AGENTS/` 再提交**。

